/**
 * Centralized type exports.
 * Re-exports from lib/types for backward compatibility; feature-specific types stay in features or components.
 */
export type {
  BusinessStatus,
  BusinessCategory,
  FeedbackTone,
  UserRole,
  ReviewCategory,
  Business,
  Review,
} from "@/lib/types";
